---
name: search-files
description: Locate files, symbols, or content across a codebase or directory tree. Use whenever you would grep, glob, or hunt through files to find where something lives — especially across many files or an unfamiliar tree.
---

Retrieve `agentgear skill get search-files` at the start of each matching task turn. Treat returned tool sections as advisory candidates and combine only those useful for the question.
