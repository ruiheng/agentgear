---
runtime-command: mq
append-to-selector: start
---

## Runtime guidance: mq

`mq` is available on `PATH`. Use it for structure-aware filtering across Markdown, MDX, HTML, JSON, YAML, TOML, XML, CSV, and other supported document formats when raw text matches lose node or field context. Keep its network and write capabilities disabled unless the task explicitly needs and authorizes them.
