---
name: multi-agent-protocol
description: Use the shared protocol for multi-agent collaboration.
---

# multi-agent-protocol

Use the shared protocol for multi-agent collaboration.

Retrieve the complete instructions with:

`agentgear skill get multi-agent-protocol start`

Follow the returned text. Repeat that command after compaction; do not open installation-specific reference paths.
