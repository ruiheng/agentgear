interface:
  display_name: "Fix Strategy"
  short_description: "Assess problem leads and decide what merits fixing"
  default_prompt: "Use $fix-strategy to verify these problem leads, decide what is worth fixing, and recommend a coordinated strategy without executing it."
policy:
  allow_implicit_invocation: false
