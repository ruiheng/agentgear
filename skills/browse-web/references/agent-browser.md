---
runtime-command: agent-browser
append-to-selector: start
---

## Runtime guidance: agent-browser

Use `agent-browser` for rendered, interactive, or JavaScript-dependent pages: `open` → `snapshot -i` → element interaction. Collect only the requested evidence.
