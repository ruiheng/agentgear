---
name: delegate-task
description: Delegate a bounded outcome to an appropriate worker surface, optionally directing the worker to run a named skill.
---

# delegate-task

Delegate a bounded outcome to an appropriate worker surface, optionally directing the worker to run a named skill.

Retrieve the complete instructions with:

`agentgear skill get delegate-task start`

Follow the returned text. Repeat that command after compaction; do not open installation-specific reference paths.
