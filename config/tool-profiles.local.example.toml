# Run the resolver's --init-local-config command to copy this file, then
# uncomment and edit only the overrides you need.
# User-local overrides load before project-local overrides.

# Override a shared command fragment.
# [templates]
# codex_approval = "--ask-for-approval never"

# Point a role at a local profile.
# [roles]
# reviewer = "reviewer_local"
# architect = "architect_local"
# This also applies to architect_author and architect_reviewer unless this file overrides either.
# Override design_pruner explicitly when a different pruning profile is wanted.

# [profiles.reviewer_local]
# [[profiles.reviewer_local.candidates]]
# command = "codex --model gpt-5.5 -c model_reasoning_effort=medium ${templates.codex_approval}"
# thurbox_agent_key = "codex" # Must match this user's Thurbox agents.toml.
#
# [[profiles.reviewer_local.candidates]]
# command = "claude --model sonnet ${templates.claude_edits}"
# thurbox_agent_key = "claude"

# Add a candidate without replacing the shared profile.
# [profiles.coder_default]
# merge = "append"
# candidates = ["claude --model sonnet ${templates.claude_edits}"]
